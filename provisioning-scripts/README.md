# Provisioning Scripts (placeholder)

This folder contains a placeholder README that links to the central provisioning scripts repository used by Digital Health SA IT teams.

Canonical repo: <https://git.example.local/dhsa/provisioning-scripts>

Purpose:

- Store approved packaging scripts and signed artifacts
- Provide example manifests and packaging checklists
- Track packaging/QA results

See `app-manifest.example.yaml` for a sample app manifest format (add manifests to this repo when packaging for production).
